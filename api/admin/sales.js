import { supabase } from '../../lib/supabase.js'
import { requireAdmin } from '../../lib/server/_shared.js'
export default async function handler(req,res){if(req.method!=='GET')return res.status(405).json({message:'Method not allowed'});if(!await requireAdmin(req,res))return;try{const {data,error}=await supabase.from('orders').select('*').order('created_at',{ascending:false}).limit(2000);if(error)throw error;const q=String(req.query.q||'').toLowerCase().trim();const items=(data||[]).filter(o=>!q||String(o.order_id||'').toLowerCase().includes(q)||String(o.customer_name||'').toLowerCase().includes(q)).map(o=>({orderId:o.order_id,customerName:o.customer_name,customerPhone:o.customer_phone,total:Number(o.total||0),paymentMethod:o.payment_method,paymentStatus:o.payment_status||o.status,createdAt:o.created_at,items:o.items||[],tableId:o.table_id||null,note:o.note||''}));res.status(200).json({items})}catch(e){res.status(500).json({message:e.message})}}
